<!-- ═══ Creative Features — Bento Grid Layout ═══ -->
<section class="section-band site-shell" id="creative-features" aria-labelledby="features-heading">
    <div class="bento-header reveal">
        <p class="eyebrow">Trai nghiem vuot troi</p>
        <h2 id="features-heading">Y te thong minh, thiet ke cho su an tam cua ban.</h2>
        <p class="bento-description">
            Moi tinh nang deu duoc toi uu hoa de mang lai trai nghiem y te truc tuyen muot ma,
            than thien va xoa bo moi rao can.
        </p>
    </div>

    <div class="bento-grid">
        <!-- Card 1: Large (spans 2 columns) -->
        <div class="bento-card bento-card-large reveal stagger-1">
            <div class="bento-icon tone-sky">
                <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
                    <polyline points="14 2 14 8 20 8"></polyline>
                    <line x1="16" y1="13" x2="8" y2="13"></line>
                    <line x1="16" y1="17" x2="8" y2="17"></line>
                </svg>
            </div>
            <div class="bento-card-content">
                <h3>Benh an dien tu hop nhat</h3>
                <p>Toan bo lich su kham, xet nghiem va don thuoc duoc luu tru an toan, de dang truy cap moi luc. He thong dong bo tu dong giua cac co so y te.</p>
            </div>
            <div class="bento-shimmer shimmer" aria-hidden="true"></div>
        </div>

        <!-- Card 2: AI Health Tracking -->
        <div class="bento-card reveal stagger-2">
            <div class="bento-icon tone-mint">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M22 12h-4l-3 9L9 3l-3 9H2"></path>
                </svg>
            </div>
            <h3>Theo doi chi so AI</h3>
            <p>He thong tu dong phan tich cac chi so suc khoe cua ban va dua ra canh bao som khi co bat thuong.</p>
        </div>

        <!-- Card 3: Prescription & Delivery -->
        <div class="bento-card reveal stagger-3">
            <div class="bento-icon tone-coral">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
                    <circle cx="12" cy="10" r="3"></circle>
                </svg>
            </div>
            <h3>Don thuoc &amp; Giao tan nha</h3>
            <p>Bac si ke don truc tuyen sau buoi kham va thuoc se duoc nha thuoc doi tac giao den tan nha ban.</p>
        </div>

        <!-- Card 4: Smart Scheduling -->
        <div class="bento-card reveal stagger-4">
            <div class="bento-icon tone-lavender">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
                    <line x1="16" y1="2" x2="16" y2="6"></line>
                    <line x1="8" y1="2" x2="8" y2="6"></line>
                    <line x1="3" y1="10" x2="21" y2="10"></line>
                    <circle cx="12" cy="15" r="2"></circle>
                </svg>
            </div>
            <h3>Dat lich thong minh</h3>
            <p>AI tu dong goi y khung gio phu hop nhat dua tren lich trinh cua ban va tinh trang suc khoe.</p>
        </div>

        <!-- Card 5: Security -->
        <div class="bento-card reveal stagger-5">
            <div class="bento-icon tone-rose">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
                    <polyline points="9 12 11 14 15 10"></polyline>
                </svg>
            </div>
            <h3>Bao mat da lop</h3>
            <p>Du lieu suc khoe duoc ma hoa AES-256, xac thuc 2 yeu to va tuan thu HIPAA de bao ve quyen rieng tu.</p>
        </div>

        <!-- Card 6: 24/7 Support -->
        <div class="bento-card reveal stagger-6">
            <div class="bento-icon tone-sky">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                    <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"></path>
                    <line x1="9" y1="10" x2="15" y2="10"></line>
                </svg>
            </div>
            <h3>Ho tro 24/7</h3>
            <p>Doi ngu tu van vien truc 24/7, san sang ho tro ban bat cu luc nao ve moi van de suc khoe.</p>
        </div>
    </div>
</section>

<style>
    /* ── Bento Header ── */
    .bento-header {
        text-align: center;
        margin-bottom: 4rem;
        max-width: 700px;
        margin-left: auto;
        margin-right: auto;
    }

    .bento-header .eyebrow {
        justify-content: center;
    }

    .bento-header h2 {
        max-width: 30ch;
        margin: 0 auto;
    }

    .bento-description {
        max-width: 60ch;
        margin: 1rem auto 0;
        color: var(--color-muted);
        font-size: 1.05rem;
        line-height: 1.7;
    }

    /* ── Bento Grid Layout ── */
    .bento-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 1rem;
    }

    /* ── Bento Card Base ── */
    .bento-card {
        position: relative;
        background: var(--glass-bg);
        backdrop-filter: var(--glass-blur-light);
        -webkit-backdrop-filter: var(--glass-blur-light);
        border: 1px solid var(--glass-border);
        border-radius: var(--radius-lg);
        padding: 2rem 1.75rem;
        display: flex;
        flex-direction: column;
        align-items: flex-start;
        overflow: hidden;
        transition: transform var(--motion-standard),
                    box-shadow var(--motion-standard),
                    border-color var(--motion-standard);
    }

    .bento-card:hover {
        transform: translateY(-6px);
        box-shadow: var(--shadow-card-hover);
        border-color: rgba(37, 99, 235, 0.15);
    }

    /* ── Large Card (spans 2 columns) ── */
    .bento-card-large {
        grid-column: span 2;
        flex-direction: row;
        align-items: center;
        gap: 2rem;
        padding: 2.5rem 2.25rem;
    }

    .bento-card-large .bento-icon {
        width: 72px;
        height: 72px;
        flex-shrink: 0;
    }

    .bento-card-large .bento-icon svg {
        width: 32px;
        height: 32px;
    }

    .bento-card-content h3 {
        font-size: 1.3rem;
        margin-bottom: 0.75rem;
    }

    /* ── Shimmer decorative element ── */
    .bento-shimmer {
        position: absolute;
        top: 0;
        right: 0;
        width: 200px;
        height: 100%;
        opacity: 0.5;
        pointer-events: none;
    }

    /* ── Bento Icon ── */
    .bento-icon {
        width: 56px;
        height: 56px;
        border-radius: var(--radius-card);
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.25rem;
        color: var(--color-primary-deep);
        box-shadow: var(--shadow-soft);
        transition: transform var(--motion-fast);
    }

    .bento-card:hover .bento-icon {
        transform: scale(1.08);
    }

    .bento-icon.tone-sky { background: var(--color-sky); color: var(--color-primary); }
    .bento-icon.tone-mint { background: var(--color-mint); color: var(--color-secondary-deep); }
    .bento-icon.tone-coral { background: var(--color-coral-soft); color: var(--color-accent-deep); }
    .bento-icon.tone-lavender { background: var(--color-lavender); color: #6d28d9; }
    .bento-icon.tone-rose { background: var(--color-rose); color: var(--color-danger); }

    /* ── Card Typography ── */
    .bento-card h3 {
        font-size: 1.15rem;
        margin-bottom: 0.6rem;
        color: var(--color-ink);
    }

    .bento-card p {
        font-size: 0.92rem;
        color: var(--color-muted);
        margin: 0;
        line-height: 1.6;
    }

    /* ── Responsive ── */
    @media (max-width: 1024px) {
        .bento-grid {
            grid-template-columns: repeat(2, 1fr);
        }

        .bento-card-large {
            grid-column: span 2;
        }
    }

    @media (max-width: 768px) {
        .bento-grid {
            grid-template-columns: 1fr;
        }

        .bento-card-large {
            grid-column: span 1;
            flex-direction: column;
            align-items: flex-start;
        }
    }
</style>
