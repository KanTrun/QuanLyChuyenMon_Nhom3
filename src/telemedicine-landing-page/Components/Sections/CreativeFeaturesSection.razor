<section class="section-band site-shell" id="creative-features" aria-labelledby="features-heading">
    <div class="section-heading text-center reveal" style="margin-bottom: 4rem; justify-items: center; text-align: center;">
        <p class="eyebrow" style="justify-content: center;">Trải nghiệm vượt trội</p>
        <h2 id="features-heading" style="max-width: 30ch;">Y tế thông minh, thiết kế cho sự an tâm của bạn.</h2>
        <p style="max-width: 60ch; margin: 1rem auto 0;">Mọi tính năng đều được tối ưu hóa để mang lại trải nghiệm y tế trực tuyến mượt mà, thân thiện và xóa bỏ mọi rào cản.</p>
    </div>

    <div class="features-grid">
        <div class="feature-card reveal stagger-1">
            <div class="feature-icon tone-sky">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path><polyline points="14 2 14 8 20 8"></polyline><line x1="16" y1="13" x2="8" y2="13"></line><line x1="16" y1="17" x2="8" y2="17"></line><polyline points="10 9 9 9 8 9"></polyline></svg>
            </div>
            <h3>Bệnh án điện tử hợp nhất</h3>
            <p>Toàn bộ lịch sử khám, xét nghiệm và đơn thuốc được lưu trữ an toàn, dễ dàng truy cập mọi lúc.</p>
        </div>

        <div class="feature-card reveal stagger-2">
            <div class="feature-icon tone-mint">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 12h-4l-3 9L9 3l-3 9H2"></path></svg>
            </div>
            <h3>Theo dõi chỉ số AI</h3>
            <p>Hệ thống tự động phân tích các chỉ số sức khỏe của bạn và đưa ra cảnh báo sớm khi có bất thường.</p>
        </div>

        <div class="feature-card reveal stagger-3">
            <div class="feature-icon tone-coral">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"></rect><circle cx="8.5" cy="8.5" r="1.5"></circle><polyline points="21 15 16 10 5 21"></polyline></svg>
            </div>
            <h3>Đơn thuốc & Giao tận nhà</h3>
            <p>Bác sĩ kê đơn trực tuyến sau buổi khám và thuốc sẽ được nhà thuốc đối tác giao đến tận nhà bạn.</p>
        </div>
    </div>
</section>

<style>
    .features-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
    }
    
    .feature-card {
        background: var(--color-surface);
        border: 1px solid var(--color-warm-gray);
        border-radius: var(--radius-lg);
        padding: 2.5rem 2rem;
        transition: transform var(--motion-standard), box-shadow var(--motion-standard), border-color var(--motion-standard);
        display: flex;
        flex-direction: column;
        align-items: flex-start;
    }
    
    .feature-card:hover {
        transform: translateY(-6px);
        box-shadow: var(--shadow-medium);
        border-color: var(--color-primary-light);
    }
    
    .feature-icon {
        width: 56px;
        height: 56px;
        border-radius: var(--radius-card);
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 1.5rem;
        color: var(--color-primary-deep);
        box-shadow: var(--shadow-soft);
    }
    
    .feature-card h3 {
        font-size: 1.25rem;
        margin-bottom: 0.75rem;
        color: var(--color-ink);
    }
    
    .feature-card p {
        font-size: 0.95rem;
        color: var(--color-muted);
        margin: 0;
        line-height: 1.6;
    }
    
    .text-center {
        text-align: center;
    }
</style>
